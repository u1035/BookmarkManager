<Window x:Class="BookmarkManager.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:BookmarkManager.Models"
        mc:Ignorable="d" WindowStyle="ToolWindow" ResizeMode="NoResize"
        SizeToContent="WidthAndHeight" ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
        Title="BookmarkManager settings" Height="450" Width="800" d:DataContext="{d:DesignInstance models:Configuration}">
    <Grid>
        <StackPanel Orientation="Vertical">

            <CheckBox Margin="4" IsChecked="{Binding RunOnWidowsStart, Mode=TwoWay}">Start with Windows</CheckBox>

            <CheckBox Margin="4" IsChecked="{Binding OpenLastUsedFile, Mode=TwoWay}">Open last used file</CheckBox>

            <CheckBox Margin="4" IsChecked="{Binding StartMinimized, Mode=TwoWay}">Start minimized</CheckBox>

            <CheckBox Margin="4" IsChecked="{Binding ShowInTaskbar, Mode=TwoWay}">Show in taskbar</CheckBox>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Margin="4">TOR browser path:</TextBlock>
                <TextBox x:Name="TorPathTextBox" Grid.Column="1" Margin="4" MinWidth="400" Text="{Binding TorBrowserPath, Mode=TwoWay}"></TextBox>
                <Button Grid.Column="1" Margin="4" HorizontalAlignment="Right" Width="30" Click="TorBrowse_OnClick">...</Button>
            </Grid>

            <Button Margin="4" Width="200" HorizontalAlignment="Right">Clear last used files list</Button>

            <StackPanel Margin="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Margin="4" MinWidth="80" Click="SaveButton_OnClick">Save</Button>
                <Button Margin="4" MinWidth="80" Click="CancelButton_OnClick">Cancel</Button>
            </StackPanel>

        </StackPanel>
    </Grid>
</Window>
